/// Bật/tắt tính năng chơi online (Firebase).
///
/// Mặc định = false để app chạy được ngay (AI + 2 người 1 máy) khi CHƯA
/// cấu hình Firebase. Sau khi chạy `flutterfire configure` (xem FIREBASE_SETUP.md),
/// đổi giá trị này thành `true` để bật chế độ online.
const bool kFirebaseEnabled = true;
